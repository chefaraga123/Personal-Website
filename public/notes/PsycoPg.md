[[Databases]], [[Postgres database]]

`Psycopg` is a PostgreSQL database adapter for python. It allows Python applications to connect to & interact with PostgreSQL databases using SQL. 
We are using `Psycopg2`
